name: whispers
description: "ECHO"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  audioplayers: ^6.0.0
  shared_preferences: ^2.2.3
  google_fonts: ^6.1.0
  sensors_plus: ^6.0.1
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  flutter_tts: ^4.2.0
  share_plus: ^10.0.2
  screenshot: ^3.0.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/dialogue/